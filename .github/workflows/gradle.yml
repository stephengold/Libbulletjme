name: CI at GitHub
on: [push]

jobs:
  Java8-Linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - run: sudo apt-get install -y g++-multilib
    - run: ./gradlew build --console=plain --no-daemon
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html

#  Java8-Linux-ARM:
#    runs-on: ubuntu-latest
#    container:
#      image: riccardoblb/buildenv-jme3:linuxArm
#    steps:
#    - uses: actions/checkout@v2
#    - uses: actions/setup-java@v1
#      with:
#        java-version: 1.8
#    - run: ./gradlew assemble --console=plain --no-daemon -Ptravis=arm64

  Java8-MacOSX64:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - run: ./gradlew build --console=plain --no-daemon -Pgithub=xcode11
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html

  Java8-Windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - run: ./gradlew build --console=plain --no-daemon
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html
