name: CI at GitHub
on: [push]

jobs:
  Java8-Linux:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 8
    - uses: gradle/wrapper-validation-action@v1
    - run: sudo apt-get update
    - run: sudo apt-get install -y g++-multilib
    - run: ./gradlew build --console=plain --no-daemon -Pflavor=Sp
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html

#  Java8-Linux-ARM:
#    if: contains(toJson(github.event.commits), '[ci skip] ') == false
#    runs-on: ubuntu-latest
#    container:
#      image: riccardoblb/buildenv-jme3:linuxArm
#    steps:
#    - uses: actions/checkout@v2
#    - uses: actions/setup-java@v1
#      with:
#        java-version: 8
#    - uses: gradle/wrapper-validation-action@v1
#    - run: ./gradlew assemble --console=plain --no-daemon -Pflavor=Sp -Ptravis=arm64

  Java8-MacOSX:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 8
    - uses: gradle/wrapper-validation-action@v1
    - run: ./gradlew build --console=plain --no-daemon -Pflavor=Sp -Pgithub=xcode11
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html

  Java8-Windows:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 8
    - uses: gradle/wrapper-validation-action@v1
    - run: ./gradlew build --console=plain --no-daemon -Pflavor=Sp
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html

  Java11-Linux:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 11
    - uses: gradle/wrapper-validation-action@v1
    - run: sudo apt-get update
    - run: sudo apt-get install -y g++-multilib
    - run: ./gradlew build --console=plain --no-daemon -Pflavor=Sp
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html

  Java11-MacOSX:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 11
    - uses: gradle/wrapper-validation-action@v1
    - run: ./gradlew build --console=plain --no-daemon -Pflavor=Sp -Pgithub=xcode11
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html

  Java11-Windows:
    if: contains(toJson(github.event.commits), '[ci skip] ') == false
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v1
      with:
        java-version: 11
    - uses: gradle/wrapper-validation-action@v1
    - run: ./gradlew build --console=plain --no-daemon -Pflavor=Sp
    - if: failure()
      run: cat build/reports/tests/test/classes/TestLibbulletjme.html
