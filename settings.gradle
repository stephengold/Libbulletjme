pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral() // to find plugins released to the Maven Central repository
    }
}

rootProject.name = 'Libbulletjme'